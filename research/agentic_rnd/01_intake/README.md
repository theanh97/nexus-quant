# Intake

Lưu research đã chuẩn hoá metadata.

Tên file gợi ý:
- `YYYY-MM-DD.source.topic.slug.json`

Metadata tối thiểu:
- `source`
- `published_at`
- `ingested_at`
- `title`
- `url`
- `summary`
- `keywords`
- `trust_tier` (`A`/`B`/`C`)
